from setuptools import setup, find_packages

setup(
    name="hello_world_steve",
    version="0.1.1",
    packages=find_packages(),
    install_requires=[
        # Add any additional package dependencies here, if needed.
    ],
)

